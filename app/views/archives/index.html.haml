= render partial: "year_switcher"

%h1
  = "Archives #{@year}"
  = render partial: "shared/account_switcher"

.row
  %table.table.table-hover
    %tbody
    - @archives.each do |archive|
      %tr
        %td= Date::MONTHNAMES[archive.month]
        %td= archive_action_link(archive)
